// Common timing constants
export const FETCH_INTERVAL = 15 * 60 * 1000; // 15 minutes in milliseconds
export const ERROR_RETRY_INTERVAL = 60 * 1000; // 1 minute retry on error
export const MEMORY_CHECK_INTERVAL = 5 * 60 * 1000; // 5 minutes for memory monitoring
export const MIN_CHECK_INTERVAL = 60 * 1000; // Minimum 1 minute between checks
export const REQUEST_TIMEOUT = 45 * 1000; // 45 second timeout for HTTP requests
export const LONG_RETRY_INTERVAL = 10 * 60 * 1000; // 10 minutes for long retry delay
